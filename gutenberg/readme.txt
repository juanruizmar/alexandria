This package contains some styles for formating LaTeX documents.

It would be good to include a brief tutorial for configurating LaTeX, in order
to make the compiler detect automatically those styles and templates.

This folder contains some basic code of a project to send messages in morse code with an Arduino Board.

At this momment, the only code it sends is hard-coded inside the "loop()" function, and it was never tested beyond simulators.
